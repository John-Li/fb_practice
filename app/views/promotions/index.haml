%h1 Promotions
%table.sample
  %tr
    %th Id
    %th Company Name
    %th Logo
    %th Title
    %th Desc
    %th Start time
    %th End time
  - @promotions.each do |promotion|
    %tr
      %td
        = promotion.id
      %td
        = promotion.company_name
      %td
        = image_tag(promotion.logo.to_s)
      %td 
        = link_to promotion.title, promotion_path(promotion)
      %td 
        = promotion.desc
      %td 
        = promotion.start_time
      %td 
        = promotion.end_time
  = link_to "Create new", new_promotion_path